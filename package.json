{
  "name": "farm-bien",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backBien && npm install && cd ../frontFarm && npm install && npm run build -- --configuration production && node ../copy-dist.js"
  }
}
