services:
  - type: web
    name: farmacia-bienestar
    env: node
    plan: starter
    branch: main
    autoDeploy: true

    buildCommand: |
      cd backBien
      npm ci
      cd ../frontFarm
      npm ci
      npm run build -- --configuration production
      cd ..
      node copy-dist.js

    startCommand: node backBien/server.js

    disk:
      name: uploads
      mountPath: /opt/render/project/src/backBien/uploads
      sizeGB: 20

    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false

